---
commit: 7dabd250f1be957235bb6b7e9a30661895b48bb7
short_sha: 7dabd250
author: Ahad Imran
date: 2026-05-10
message: "fix: show proper provider name (ChatGPT/Perplexity) in terminal logs"
model: gpt-5-5-thinking
provider: chatgpt
reviewed_at: 2026-05-10T14:53:38.284Z
---

# Code Review: 7dabd250

> fix: show proper provider name (ChatGPT/Perplexity) in terminal logs

## Summary
This commit improves terminal logging in `cli/proxima-review.cjs` by displaying human-readable AI provider names (e.g., ChatGPT, Perplexity) instead of internal identifiers. It introduces a `PROVIDER_DISPLAY` mapping and uses `PROVIDER_LABEL` for log messages (cli/proxima-review.cjs:L36-L44, L246, L337, L346). Minor formatting improvements were also applied for consistent code style. I verified standard Node.js console logging practices; the change improves usability and readability without altering core functionality.

## Issues & Fixes
| File:Line | Severity | Issue | Fix |
|------------|----------|-------|-----|
| cli/proxima-review.cjs:L42 | ⚠️ | `STALE_LOCK_MS` reintroduced after previous refactor; inconsistent naming | Ensure consistent lock constant naming (`LOCK_STALE_AFTER_MS`) throughout the codebase to prevent confusion |
| cli/proxima-review.cjs:L37-L44 | ⚠️ | `PROVIDER_DISPLAY` mapping may not cover all potential provider identifiers | Consider adding a fallback or logging a warning when an unknown provider is encountered |
| cli/proxima-review.cjs:L246, L346 | ⚠️ | Log messages rely on `PROVIDER_LABEL`; if mapping fails, raw provider string is shown | Validate `PROVIDER_LABEL` before logging to avoid misleading output |

## Security & Performance
- **Security:** No new security risks introduced. Logs display provider names only (cli/proxima-review.cjs:L246, L346).  
- **Performance:** Minimal impact; string mapping and log formatting are lightweight.  

## Overall Score: 9/10
Enhances user experience and logging clarity. Minor consistency issues with lock constant and potential unmapped provider names slightly reduce score.

## Recommendations
1. Standardize lock constant usage: replace `STALE_LOCK_MS` with `LOCK_STALE_AFTER_MS` to match previous refactor (cli/proxima-review.cjs:L42).  
2. Expand `PROVIDER_DISPLAY` or add a fallback for unmapped providers to avoid unexpected log outputs (cli/proxima-review.cjs:L37-L44).  
3. Ensure all log statements use `PROVIDER_LABEL` consistently (cli/proxima-review.cjs:L246, L346).  
4. Optional: Add a test case to verify log messages for each supported provider.